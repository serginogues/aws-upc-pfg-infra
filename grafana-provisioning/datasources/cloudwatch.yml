apiVersion: 1

datasources:
  - name: CloudWatch
    type: cloudwatch
    access: proxy
    url: ""
    jsonData:
      authType: keys
      defaultRegion: ${region}
      accessKey: ${access_key}
      secretKey: ${secret_key}
    isDefault: true
    editable: true
    uid: cloudwatch
